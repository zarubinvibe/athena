# {{ПРОЕКТ}} — проектная конституция

> ≤150 строк. Личное/глобальные правила — в ~/.claude (наследуются). Здесь только специфика проекта.

## Что за проект
{{1-2 строки: цель, тип (web/RN/python/lib), стадия}}

## Стек и команды
- Стек: {{...}}
- Установка: `{{install}}`
- Запуск: `{{run}}`
- Тесты: `{{test}}`
- Линт/типы: `{{lint}}`
- Деплой: см. ~/.claude/references/deploy.md (если на сервер)

## Архитектура (gotchas)
- {{ключевые модули, неочевидные ловушки, инварианты}}

## Правила проекта
- Выверять модель под задачу (глобальное жесткое правило).
- Секреты — только `.env`/secret manager, никогда в git (см. ~/.claude/references/secrets-map.md).
- specs первыми → код → evals (гейт). Файлы ≤800 строк, функции ≤50.
- Перед новым кодом: GitHub search + Context7 (доки/код) → переиспользовать, не писать с нуля.

## Структура
- `INDEX.md` — роутер по проекту (1 строка на элемент)
- `specs/` — цели, DoD, ограничения · `src/` — код · `evals/` — тесты/трейсы · `docs/`
- `.claude/` — rules/agents/skills/commands проекта

## Workflow
`/grill-me` → план → утверждение → реализация. Фазовый план: `gsd-new-project`.
