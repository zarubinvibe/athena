# Секреты — никогда в git
.env
.env.*
*secret*
*credential*
*.pem
id_rsa*

# Артефакты
node_modules/
dist/
build/
runs/
.firecrawl/
__pycache__/
*.log
.DS_Store

# Карта секретов (если положена в проект)
**/references/secrets-map.md

# Athena
*.local
# Личные манифесты — реальные живут в приватном overlay, не в generic-репо
projects.manifest
tools.manifest
# Локальный конфиг с приватными данными владельца — канал утечки (аудит P0.1, KGB-1)
athena.config.sh
# chezmoi data с боевыми значениями (vps_host и пр.) — если положен реальный файл (KGB-19)
chezmoidata.yaml
.chezmoidata.yaml
.secrets/
secrets/
*.env
!*.env.example
chezmoi/**/private_*
.skills-snapshot.tar.zst
smoke/*.log

# Локальный IDE-конфиг
.vscode/

# Knowledge graph rebuilt on demand; never committed.
graphify-out/
